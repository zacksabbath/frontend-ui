---
name: Template-Default
route: /Templates/Default
---

import { Playground, Props } from 'docz';
import DefaultTemplate from './DefaultTemplate';
import { Content, Header, Footer } from './SampleContent';
import { theme } from '../../../context';
import { ThemeProvider } from 'styled-components';

# Default Template

## Props

<Props of={DefaultTemplate} />

## Content with Header / Footer

<Playground>
  <ThemeProvider theme={theme}>
    <DefaultTemplate
      header={<Header />}
      content={<Content />}
      footer={<Footer />}
    />
  </ThemeProvider>
</Playground>

## Content with Header only

<Playground>
  <ThemeProvider theme={theme}>
    <DefaultTemplate header={<Header />} content={<Content />} />
  </ThemeProvider>
</Playground>
